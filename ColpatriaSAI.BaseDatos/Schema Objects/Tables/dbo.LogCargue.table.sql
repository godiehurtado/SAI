CREATE TABLE [dbo].[LogCargue] (
    [id]             INT            IDENTITY (1, 1) NOT NULL,
    [descripcion]    VARCHAR (2000) NULL,
    [cargue_id]      INT            NULL,
	[cargue_tipo]    INT            NULL
);
